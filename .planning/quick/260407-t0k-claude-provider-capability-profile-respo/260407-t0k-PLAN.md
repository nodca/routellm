---
phase: quick-260407-t0k-claude-provider-capability-profile-respo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/claude/mod.rs
  - src/claude/provider_capability_profile.rs
  - src/claude/responses_adapter.rs
  - src/http.rs
autonomous: true
requirements:
  - quick-260407
user_setup: []
must_haves:
  truths:
    - "Claude semantic request 里的扩展字段不会再被 `ResponsesProviderAdapter` 以固定写死策略处理，而是先经过显式的 provider capability profile 判定。"
    - "Responses adapter 会按上游能力分层处理扩展字段：支持的字段被转发，不支持但可静默忽略的字段被显式标记，不应透传的 beta / capability 请求有可测试的归类结果。"
    - "`/v1/messages -> semantic_core -> provider capability profile -> Responses adapter -> existing responses dispatch` 这条链继续成立，不会为了扩展字段处理再长出第二套 Claude upstream 核心。"
  artifacts:
    - path: src/claude/provider_capability_profile.rs
      provides: "Claude upstream/provider capability profile contracts plus extension handling decisions"
    - path: src/claude/responses_adapter.rs
      provides: "Responses adapter that derives extension forwarding/ignoring/omitting from a provider capability profile"
    - path: src/http.rs
      provides: "Prepared Claude message payload wiring that instantiates the profile-backed adapter for standard and compat dispatch"
  key_links:
    - from: src/claude/semantic_core.rs
      to: src/claude/provider_capability_profile.rs
      via: "typed Claude request extensions are resolved against provider capabilities before payload mapping"
      pattern: "ClaudeRequestExtensions"
    - from: src/claude/provider_capability_profile.rs
      to: src/claude/responses_adapter.rs
      via: "Responses adapter computes extension handling from the Responses capability profile instead of hard-coded field rules"
      pattern: "ResponsesProviderAdapter"
    - from: src/http.rs
      to: src/claude/responses_adapter.rs
      via: "prepared Claude message payloads reuse the profile-backed adapter in both standard and assistant-history compat modes"
      pattern: "build_claude_message_payloads"
---

<objective>
为 Claude 原生网关补上 provider capability profile，让 Responses adapter 按上游能力显式处理扩展字段。

Purpose: 在已经完成的 `semantic_core -> responses_adapter` seam 上，再补一层稳定的能力画像边界，避免扩展字段处理继续散落成 “Responses 特判集合”，为以后新增 provider 或扩大 Claude 扩展支持留出干净落点。
Output: 一份原子 quick plan，新增 provider capability profile 契约、让 Responses adapter 基于该画像分层处理扩展字段，并把这层能力接回现有 Claude message payload 准备链与回归测试。
</objective>

<execution_context>
@/home/wcn/.codex/get-shit-done/workflows/quick.md
@/home/wcn/.codex/get-shit-done/workflows/execute-plan.md
@/home/wcn/.codex/get-shit-done/templates/summary.md

Scope guard for this quick slice:
- 这是 Claude native gateway 的扩展字段能力收束，不是完整多 provider 选择系统。
- 只为当前已存在的 `ResponsesProviderAdapter` 建立 capability profile；不新增新的 upstream dispatch core，不改 route/store/schema。
- 保持项目锁定决策：真正的上游核心仍然是 `/v1/responses`，兼容层继续做薄。
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/quick/260407-ryn-claude-claude-semantic-core-responses-pr/260407-ryn-SUMMARY.md
@.planning/quick/260407-ryn-claude-claude-semantic-core-responses-pr/260407-ryn-PLAN.md
@src/claude/semantic_core.rs
@src/claude/responses_adapter.rs
@src/http.rs

Locked project decisions to honor:
- 上游协议统一收敛到 `/v1/responses`
- Claude `/v1/messages` 是兼容边界，不是第二套路由核心
- 兼容层必须是薄层；如果扩展字段逻辑开始反向污染核心模型，应优先抽边界而不是继续加条件分支

<interfaces>
From src/claude/semantic_core.rs:
```rust
pub struct ClaudeMessageRequest {
    pub model: String,
    pub messages: Vec<ClaudeMessage>,
    pub system: Option<ClaudeSystemPrompt>,
    pub stream: bool,
    pub max_tokens: Option<u64>,
    pub temperature: Option<f64>,
    pub top_p: Option<f64>,
    pub tools: Vec<ClaudeToolDefinition>,
    pub tool_choice: Option<ClaudeToolChoice>,
    pub extensions: ClaudeRequestExtensions,
}

pub struct ClaudeRequestExtensions {
    pub thinking: Option<ClaudeThinkingConfig>,
    pub context_management: Option<ClaudeContextManagement>,
    pub metadata: Option<Map<String, Value>>,
    pub beta_hints: ClaudeBetaHints,
    pub request_hints: ClaudeRequestHints,
}
```

From src/claude/responses_adapter.rs:
```rust
pub struct ResponsesProviderAdapter {
    request_mode: ResponsesRequestMode,
}

impl ResponsesProviderAdapter {
    pub fn extension_policy(&self, request: &ClaudeMessageRequest) -> ResponsesExtensionPolicy;
    pub fn request_to_payload(&self, request: &ClaudeMessageRequest) -> Result<Value, AppError>;
}
```

From src/http.rs:
```rust
fn build_claude_message_payloads(payload: Value) -> Result<PreparedPayloads, AppError>

struct ClaudeMessagePayloads {
    semantic_request: ClaudeMessageRequest,
    response_adapter: ResponsesProviderAdapter,
    compat_response_adapter: ResponsesProviderAdapter,
}
```
</interfaces>

Current gap this plan closes:
- `semantic_core` 已经把 `thinking` / `context_management` / `metadata` / beta hints / `service_tier` 解析成 typed extensions。
- `responses_adapter` 也已经有 `extension_policy()` 雏形，但它还是“写死的 Responses 规则集合”，不是一个可复用、可对比、可测试的 provider capability profile。
- 下一步应该把 “字段怎么处理” 从 adapter 的具体实现里再抽一层，先表达 upstream 能力，再让 adapter 依据能力画像映射字段。
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define provider capability profile contracts for Claude extension handling</name>
  <files>src/claude/mod.rs, src/claude/provider_capability_profile.rs</files>
  <read_first>src/claude/semantic_core.rs, src/claude/responses_adapter.rs</read_first>
  <behavior>
    - A Claude request extension set can be evaluated into explicit handling decisions instead of implicit `if field present then ignore/forward` branches.
    - The profile distinguishes at least three classes of behavior for this slice: forwarded to upstream, intentionally ignored/omitted because upstream lacks support, and requested beta/capability hints that remain unsupported.
    - The Responses upstream profile for the current repo behavior is encoded in one named place, so future provider work can add another profile without rewriting Claude semantic parsing.
  </behavior>
  <action>Create `src/claude/provider_capability_profile.rs` as the new capability boundary sitting between `semantic_core` and provider adapters. Define profile types for the Claude extension slice already parsed today: metadata, service tier, thinking, context management, and beta hints. Model explicit handling outcomes rather than booleans, for example forward-to-upstream vs omit vs ignore-requested vs unsupported-beta, so downstream adapters can consume the decision without re-deriving policy from raw request fields. Add a named Responses profile that matches current intended behavior: forward metadata/service tier, do not forward thinking/context management, and surface beta/capability requests as unsupported unless later mapped. Export the new module from `src/claude/mod.rs`. Keep this task transport-agnostic and provider-selection-free: it should describe capabilities and decisions, not build JSON payloads or dispatch HTTP requests.</action>
  <verify>
    <automated>cargo test claude_provider_capability_profile --lib</automated>
  </verify>
  <done>`src/claude/provider_capability_profile.rs` exists with explicit capability-profile contracts, a named Responses capability profile, and focused tests locking the extension decision matrix for the current Claude slice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor Responses adapter to derive extension behavior from the capability profile</name>
  <files>src/claude/provider_capability_profile.rs, src/claude/responses_adapter.rs</files>
  <read_first>src/claude/provider_capability_profile.rs, src/claude/responses_adapter.rs, src/claude/semantic_core.rs</read_first>
  <behavior>
    - `ResponsesProviderAdapter` computes extension handling from the Responses capability profile instead of keeping a parallel hand-written ruleset.
    - Payload generation still forwards supported extensions like metadata/service tier, while unsupported extensions such as thinking/context management are explicitly classified but not leaked into the Responses payload.
    - The adapter exposes enough structured policy output for callers/tests to prove which Claude-native extensions were forwarded, ignored, omitted, or left unsupported.
  </behavior>
  <action>Refactor `ResponsesProviderAdapter` so its extension resolution is driven by the provider capability profile created in Task 1. Replace or reshape the current `ResponsesExtensionPolicy` into a profile-backed decision object that clearly reports how each extension field was handled for this provider. Keep the current successful payload mapping behavior for model/input/system/tools/tool_choice plus forwarded metadata/service tier. Remove duplicated policy derivation logic from the adapter wherever the profile now owns it. Preserve assistant-history compat mode as an orthogonal request-mode concern; do not entangle it with extension capability decisions. Add targeted adapter tests that prove the same semantic request yields the expected split: supported fields appear in the Responses payload, unsupported requested fields are classified but omitted, and beta hints are reported through the capability decision layer rather than as an ad-hoc raw clone list.</action>
  <verify>
    <automated>cargo test claude_responses_adapter --lib</automated>
  </verify>
  <done>`src/claude/responses_adapter.rs` no longer hard-codes extension behavior independently of a provider profile, and its tests pin the Responses-specific extension handling contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire the profile-backed adapter into Claude message payload preparation and regressions</name>
  <files>src/http.rs, src/claude/responses_adapter.rs</files>
  <read_first>src/http.rs, src/claude/provider_capability_profile.rs, src/claude/responses_adapter.rs</read_first>
  <behavior>
    - `build_claude_message_payloads` constructs the standard and assistant-history compat adapters from the same provider capability profile instead of assuming adapter-local fixed policy.
    - Existing Claude message preparation still builds valid Responses payloads for the supported slice and keeps assistant-history compat retry behavior.
    - Regression coverage shows the new capability profile is actually used in the live seam, not just unit-tested in isolation.
  </behavior>
  <action>Update the Claude message payload preparation path in `src/http.rs` so `PreparedPayloads::for_claude_message` creates profile-backed Responses adapters for both standard and compat modes. Keep external behavior unchanged for the currently supported `/v1/messages` slice: metadata/service tier continue to flow through, assistant-history compat retry remains available, and no new provider routing branch appears in HTTP orchestration. Extend the existing skeleton regression tests so they prove the prepared payload path is consuming the new capability profile semantics, not a leftover adapter-local policy. Do not change request logging, route selection, or non-Claude protocol paths in this task.</action>
  <verify>
    <automated>cargo test claude_native_gateway_skeleton --lib</automated>
  </verify>
  <done>The Claude message preparation seam is profile-backed end to end, and regressions show the new capability layer is live without changing current supported Claude gateway behavior.</done>
</task>

</tasks>

<verification>
Before declaring the quick task complete:
- [ ] `cargo fmt`
- [ ] `cargo test claude_provider_capability_profile --lib`
- [ ] `cargo test claude_responses_adapter --lib`
- [ ] `cargo test claude_native_gateway_skeleton --lib`
- [ ] `cargo test --lib`
</verification>

<success_criteria>
- Claude extension handling now has an explicit provider capability profile boundary instead of being embedded as a Responses-only adapter ruleset.
- The Responses adapter uses that profile to decide which Claude-native extensions are forwarded, ignored, omitted, or marked unsupported.
- The live Claude message preparation seam keeps current supported behavior while becoming easier to extend for future upstream capability differences.
</success_criteria>

<output>
After completion, create `/home/wcn/metapi-rs/.planning/quick/260407-t0k-claude-provider-capability-profile-respo/260407-t0k-SUMMARY.md`
</output>
