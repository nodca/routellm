---
phase: quick-260407-tc7-claude-strict-responses-compat-responses
plan: 01
type: execute
wave: 1
depends_on:
  - quick-260407-t0k
files_modified:
  - src/claude/provider_capability_profile.rs
  - src/claude/responses_adapter.rs
  - src/http.rs
autonomous: true
requirements:
  - quick-260407
user_setup: []
must_haves:
  truths:
    - "Claude `/v1/messages -> semantic_core -> responses_adapter -> /v1/responses` 主线不变，不引入第二套 upstream core。"
    - "Responses upstream 明确拆成 strict / compat 两套 capability profile。"
    - "assistant-history fallback 和降级日志由 profile 决策驱动，而不是散落在 `http.rs` 的匿名条件分支。"
  artifacts:
    - path: src/claude/provider_capability_profile.rs
      provides: "strict / compat responses capability profiles plus assistant-history fallback policy"
    - path: src/claude/responses_adapter.rs
      provides: "profile-aware assistant-history retry gating"
    - path: src/http.rs
      provides: "channel-selected Claude profile wiring, profile-aware fallback, and request-log gateway diagnostics"
---

<objective>
为 Claude 原生网关补上 strict responses / compat responses profile 分叉，并把 fallback / 降级日志绑到 profile 决策。

Purpose: 让 Claude Code 走兼容上游时能更像原生地恢复，同时把“这次请求到底用了什么 profile、有没有降级”写进现有 request log 诊断链。
Output: 一次原子 quick 实现，包含 capability profile 分叉、channel 级 profile 选择、assistant-history compat fallback 绑定、结构化日志落库与回归测试。
</objective>

<execution_context>
@/home/wcn/.codex/get-shit-done/workflows/quick.md

Scope guard:
- 不新增新的 channel schema / route schema / DB 列
- 不增加第二套 Claude upstream dispatch core
- 保持 `/v1/responses` 仍是唯一上游核心
</execution_context>

<tasks>
- 定义 `strict-responses` 与 `compat-responses` 两套 Claude provider capability profile，并补 profile 选择规则。
- 让 `ResponsesProviderAdapter` 的 assistant-history compat retry 由 profile 决定。
- 将 Claude payload 缓存改成按 profile + request mode 缓存，支持 channel 级 profile 选择。
- 在 `attempt_proxy_request()` 中把 selected channel 的 profile、fallback 状态和扩展降级结果写入 `claude_request_fingerprint.gateway`。
- 增加/更新回归测试，覆盖 compat fallback 行为与日志可见性。

<verification>
- [x] `cargo fmt`
- [x] `cargo test claude_provider_capability_profile --lib`
- [x] `cargo test claude_responses_adapter --lib`
- [x] `cargo test claude_native_gateway_skeleton --lib`
- [x] `cargo test --lib`
</verification>
